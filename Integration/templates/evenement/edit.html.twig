{% extends 'base-back.html.twig' %}

{% block title %}Éditer un événement{% endblock %}

{% block body %}

<div class="card-body">
<section class="section">
	<div class="row">
		<div class="col-lg-12">
			<div class="card">
				<div class="card-body">
					<h5 class="card-title">Editer évènement</h5>
{{ include('evenement/_form.html.twig', {'button_label': 'Mettre à jour'|trans }) }}

</div>
<div class="card-footer">
<a href="{{ path('app_evenement_index') }}" class="btn btn-secondary">Retour à la liste</a>
{{ include('evenement/_delete_form.html.twig') }}
</div>
</div>
</div>
{% endblock %}