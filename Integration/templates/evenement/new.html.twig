{% extends 'base-back.html.twig' %}

{% block title %}New Evenement{% endblock %}

{% block body %}
   
<section class="section">
	<div class="row">
		<div class="col-lg-12">
			<div class="card">
				<div class="card-body">
					<h5 class="card-title"> Nouveau evènement</h5>
        {{ include('evenement/_form.html.twig') }}

        <a href="{{ path('app_evenement_index') }}" class="btn btn-primary mt-4">back to list</a>

        
    </div>
    
{% endblock %}
