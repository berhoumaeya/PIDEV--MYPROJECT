<form method="post" action="{{ path('app_evenement_delete', {'id_event': evenement.getId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ evenement.getId) }}">
    <button type="submit" class="btn btn-danger">Delete</button>
</form>
