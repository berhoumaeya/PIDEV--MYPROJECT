{% extends 'base-front.html.twig' %}

{% block title %}Hotel{% endblock %}

{% block body %}
    <h1>Hotel</h1>
                <a href="{{ path('app_hotel_index') }}"class="btn btn-outline-secondary rounded" style=" position: absolute; top: 10px; right: 10px; margin-right: 10px; margin-top: 10px;">back to list</a>


    <table  class="table  table-hover display nowrap  border " style="width:100%">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ hotel.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ hotel.nom }}</td>
            </tr>
            <tr>
                <th>Star</th>
                <td>{{ hotel.etoile }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ hotel.type }}</td>
            </tr>
        </tbody>
    </table>

    <div class="d-flex justify-content-between">
        <a href="{{ path('app_hotel_edit', {'id': hotel.id}) }}" class="btn btn-success rounded mr-2">Edit</a>



     
            <div class="btn-group" role="group" aria-label="Actions">
                       {{ include('hotel/_delete_form.html.twig') }}

          

    </div>
 </div>
{% endblock %}
