{% extends 'base-back.html.twig' %}

{% block title %}Edit Reservation{% endblock %}

{% block body %}

<section class="section">
	<div class="row">
		<div class="col-lg-12">
			<div class="card">
				<div class="card-body">
					<h5 class="card-title">Modifier Reservation</h5>

    {{ include('reservation/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a href="{{ path('reservation_index') }}"class="btn btn-secondary">Retour à la liste</a>

    {{ include('reservation/_delete_form.html.twig') }}
{% endblock %}
