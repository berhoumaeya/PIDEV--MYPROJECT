<form method="post" action="{{ path('app_sponsor_delete', {'id_sponsor': sponsor.id_sponsor}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sponsor.id_sponsor) }}">
    <button  class="btn btn-danger">Delete</button>
</form>
