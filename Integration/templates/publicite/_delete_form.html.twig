<form method="post" action="{{ path('app_publicite_delete', {'id_pub': publicite.getId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ publicite.getId) }}">
    <button type="submit" class="btn btn-danger">Delete</button>
</form>
