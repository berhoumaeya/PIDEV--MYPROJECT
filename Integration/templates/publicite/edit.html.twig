{% extends 'base-back.html.twig' %}

{% block title %}Éditer une publicité{% endblock %}

{% block body %}

<div class="card-body">
<section class="section">
	<div class="row">
		<div class="col-lg-12">
			<div class="card">
				<div class="card-body">
					<h5 class="card-title">Editer une publicité</h5>
{{ include('publicite/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
</div>
<div class="card-footer">
<a href="{{ path('app_publicite_index') }}" class="btn btn-primary">Retour à la liste</a>
{{ include('publicite/_delete_form.html.twig') }}
</div>
</div>
</div>
</div>
</div>
{% endblock %}