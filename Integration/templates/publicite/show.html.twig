{% extends 'base-back.html.twig' %}

{% block title %}Publicite{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-body">
            <h1 class="card-title">Publicite</h1>

            <table class="table">
                <tbody>
                    <tr>
                        <th>Id_pub</th>
                        <td>{{ publicite.getId }}</td>
                    </tr>
                    <tr>
                        <th>Type</th>
                        <td>{{ publicite.getType }}</td>
                    </tr>
                </tbody>
            </table>

            <a href="{{ path('app_publicite_index') }}" class="btn bt n-primary mr-2">Retour à la liste</a>

            <div class="btn-group" role="group" aria-label="Actions">
                <a href="{{ path('app_publicite_edit', {'id_pub': publicite.getId}) }}" class="btn btn-success mr-2">Modifier</a>
                

                {{ include('publicite/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
